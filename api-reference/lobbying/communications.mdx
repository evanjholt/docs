---
title: "Communications"
api: "GET /api/v1/lobbying/communications"
description: "Retrieve communication reports filed by registered lobbyists."
---

Access records of monthly communication reports describing interactions between designated public office holders and registered lobbyists.

### Query Parameters

<ParamField query="client" type="string">
  Partial match on client organization name.
</ParamField>

<ParamField query="dpoh" type="string">
  Partial match on the designated public office holder name.
</ParamField>

<ParamField query="institution" type="string">
  Partial match on the department or institution contacted.
</ParamField>

<ParamField query="lobbyist" type="string">
  Partial match on the lobbyist name.
</ParamField>

<ParamField query="subject" type="string">
  Partial match on the subject labels or subject text.
</ParamField>

<ParamField query="date_from" type="string">
  ISO 8601 start date.
</ParamField>

<ParamField query="date_to" type="string">
  ISO 8601 end date.
</ParamField>

<ParamField query="limit" type="integer">
  Results per page. Default: `50`. Max: `500`
</ParamField>

<ParamField query="offset" type="integer">
  Result offset for pagination. Default: `0`
</ParamField>

<Note>
  `page` and `per_page` are accepted as input aliases for `limit` and offset-based pagination respectively.
</Note>

<RequestExample>

```bash cURL
curl "https://api.northaxiumdata.ca/api/v1/lobbying/communications?institution=Finance&limit=5&offset=5" \
  -H "X-API-Key: <your-api-key>"
```

```python Python
import requests

url = "https://api.northaxiumdata.ca/api/v1/lobbying/communications"
headers = {"X-API-Key": "<your-api-key>"}
params = {
    "institution": "Finance",
    "limit": 5,
    "offset": 5
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

```javascript Node.js
const fetch = require('node-fetch');

const url = new URL('https://api.northaxiumdata.ca/api/v1/lobbying/communications');
url.search = new URLSearchParams({
  institution: 'Finance',
  limit: '5',
  offset: '5'
});

fetch(url, {
  headers: {
    'X-API-Key': '<your-api-key>'
  }
})
  .then(res => res.json())
  .then(json => console.log(json));
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": [
    {
      "com_id": "COM-992381",
      "date": "2023-10-15",
      "lobbyist_name": "Jane Smith",
      "client": "Green Energy Solutions Ltd.",
      "dpoh_name": "Mark Carney",
      "dpoh_title": "Minister of Finance",
      "institution": "Department of Finance Canada",
      "subjects": "Taxation and Finance, Environment"
    }
  ],
  "meta": {
    "total": 355000,
    "limit": 5,
    "offset": 5
  }
}
```

```json 400
{
  "error": {
    "code": "BAD_REQUEST",
    "message": "Invalid parameter value."
  }
}
```

```json 401
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Missing API key. Pass your key in the X-API-Key header."
  }
}
```

```json 404
{
  "error": {
    "code": "NOT_FOUND",
    "message": "The requested resource was not found."
  }
}
```

```json 429
{
  "error": {
    "code": "RATE_LIMITED",
    "message": "Monthly request limit exceeded."
  }
}
```

</ResponseExample>
