---
title: "Communications"
api: "GET /api/v1/lobbying/communications"
description: "Retrieve communication reports filed by registered lobbyists."
---

Access records of monthly communication reports describing interactions between designated public office holders and registered lobbyists.

### Query Parameters

<ParamField query="limit" type="integer" default="100">
  Maximum number of records to return. Maximum limits map to your plan (100 for Basic, 1,000 for Pro, 10,000 for Enterprise).
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of records to skip for pagination.
</ParamField>

<ParamField query="date_start" type="string">
  Filter for communications that occurred on or after this date (`YYYY-MM-DD`).
</ParamField>

<ParamField query="date_end" type="string">
  Filter for communications that occurred on or before this date (`YYYY-MM-DD`).
</ParamField>

<ParamField query="government_institution" type="string">
  Filter by the federal department or institution contacted.
</ParamField>

<RequestExample>

```bash cURL
curl "https://api.northaxiumdata.ca/api/v1/lobbying/communications?limit=5&government_institution=Finance" \
  -H "X-API-Key: <your-api-key>"
```

```python Python
import requests

url = "https://api.northaxiumdata.ca/api/v1/lobbying/communications"
headers = {"X-API-Key": "<your-api-key>"}
params = {
    "limit": 5,
    "government_institution": "Finance"
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

```javascript Node.js
const fetch = require('node-fetch');

const url = new URL('https://api.northaxiumdata.ca/api/v1/lobbying/communications');
url.search = new URLSearchParams({
  limit: '5',
  government_institution: 'Finance'
});

fetch(url, {
  headers: {
    'X-API-Key': '<your-api-key>'
  }
})
  .then(res => res.json())
  .then(json => console.log(json));
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": [
    {
      "communication_id": "COM-992381",
      "date_of_communication": "2023-10-15",
      "lobbyist_name": "Jane Smith",
      "client": "Green Energy Solutions Ltd.",
      "public_office_holders": [
        {
          "name": "John Doe",
          "title": "Director of Technology",
          "institution": "Department of Finance Canada"
        }
      ],
      "subject_matter": ["Taxation and Finance", "Environment"]
    }
  ],
  "meta": {
    "total": 1,
    "limit": 5,
    "offset": 0
  }
}
```

```json 400
{
  "error": {
    "code": "BAD_REQUEST",
    "message": "Invalid parameter value."
  }
}
```

```json 401
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Missing API key. Pass your key in the X-API-Key header."
  }
}
```

```json 404
{
  "error": {
    "code": "NOT_FOUND",
    "message": "The requested resource was not found."
  }
}
```

```json 429
{
  "error": {
    "code": "RATE_LIMITED",
    "message": "Monthly request limit exceeded."
  }
}
```

</ResponseExample>
